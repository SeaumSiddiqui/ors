package com.rms.ors.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class ManagementController {

    // get all application

    // get application by id

    // get application from all USER

    // get application by USER

    // update application submitted by any USER on status (PENDING, SUBMITTED)
}
